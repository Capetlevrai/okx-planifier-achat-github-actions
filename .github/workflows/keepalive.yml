name: 3. 🔐 CLÉ OKX — Maintenir la connexion (aucun achat)

on:
  schedule:
    # Tous les jours à 06:00 UTC : garde active une clé OKX non liée à une IP
    # (OKX peut expirer trade sans IP fixe après ~14 jours d'inactivité).
    # Aucun ordre : uniquement un GET solde authentifié.
    - cron: '0 6 * * *'

permissions:
  contents: read

concurrency:
  group: okx-dca-keepalive
  cancel-in-progress: false

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - name: Appel OKX authentifié sans ordre
        env:
          OKX_API_KEY: ${{ secrets.OKX_API_KEY }}
          OKX_SECRET_KEY: ${{ secrets.OKX_SECRET_KEY }}
          OKX_API_SECRET: ${{ secrets.OKX_API_SECRET }}
          OKX_PASSPHRASE: ${{ secrets.OKX_PASSPHRASE }}
        run: |
          if [ -z "$OKX_API_KEY" ]; then echo "Secrets OKX absents — keepalive ignoré."; exit 0; fi
          node scripts/keepalive.mjs
